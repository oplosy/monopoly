# Plan 10 notes (cloud session, 2026-09-25)

Running record of `docs/superpowers/plans/2026-09-25-plan-10-table-plain.md` on `feat/table-plain`.
The container is ephemeral and `.superpowers/` is git-ignored, so this file is the ledger that survives.

## Environment

- No Google Chrome here. **Every `pnpm e2e` of the plan ran as** `npx playwright test -c <scratch>/pw.chromium.config.ts`
  from `apps/e2e`: the handoff's scratch config (outside the repo), Playwright's bundled Chromium at
  `/opt/pw-browsers/chromium`. `apps/e2e/playwright.config.ts` is unchanged.
- Google Fonts do not load in this browser: screenshots use fallback fonts. Layout and overlaps were judged, not type.

## Baseline (feat/table-plain = main + spec + plan)

- `pnpm test`: engine 108, protocol 6, server 59, web 410. `pnpm typecheck`, `pnpm lint`: clean.

## Tasks
- e2e baseline (Chromium via scratch config): 15 passed.

### Task 1: the motion setting — `feat: add an in-game motion setting, on by default`
- New `motion-setting.test.ts` (4), `mode.test.ts` +2; web 416. Typecheck, lint clean.
- The plan's check `git grep -n "prefers-reduced-motion" apps/web/src -- '*.ts' '*.tsx'` is not valid git syntax
  (pathspec before `--`); ran `grep -rn … --include=*.ts --include=*.tsx`: nothing.
- Ruling: `table.spec`'s "nothing flies when the OS asks for less motion" went red with this task (the game no
  longer asks the OS), and the plan only moves it to the switch in Task 4 Step 5. That step (players.ts,
  table.spec, sound.spec) was pulled forward as its own commit right after Task 2's check found it:
  `test: calm e2e tables through the motion setting, not the OS`. — CLAUDE.md: never commit with a failing
  gate — cost if wrong: none (same edits, earlier). The Task 1 commit itself was made before e2e ran, so it
  carries that one red e2e test; the next commits are green.

### Task 2: the plain table at 22° — `feat: lay a plain felt table at a 22° tilt`
- New `css.ts` helpers, `scene-css.test.ts` (3); `scene.test.tsx`, `geometry.test.ts` updated; web 418.
- Ruling: `Tabletop.tsx` already had a local component named `TableScene` (the plan's new scene clashed with
  it, TS2440). The local one is now `GameTable`. — naming only — cost if wrong: none.
- Ruling (fit values, measured in a scratch harness at 14 viewports × 6 states; Plan 11 replaces them all):
  - desktop `.tabletop .scene { --plane: min(64vw, calc((100vh - 210px) * 0.9)); --plane-shift: -8%; }`
    (the plan's starting `(100vh - 190px) * 0.92, -4%` let the pay tray cover my own cards at 1280×800 and
    1280×720, and my seat at 1280×800). — cost if wrong: a slightly smaller table on desktops.
  - While paying, at every size, the hand tucks down (`bottom: hand-w × −1.12`) and the pay tray sits at the
    bottom (12 px): at 22° the near rim is lower, and the cards to pay with must stay pickable. (This was the
    short-landscape behaviour from PR #9; now general.) — cost if wrong: the hand is only a peek while paying.
  - Short landscape (≤500 px high): a round table at 22° is almost as tall as wide, so its plane is
    `min(56vw, 100vh × 0.9)` (3 players) and `min(56vw, 100vh × 0.78)`, shift 9 % (2 players, whose far seat
    sits on the top edge); the resting hand shows a little less (`bottom: hand-w × −0.52`); the HUD stands as a
    column in the top right corner (a one-row HUD always covered the right-hand seat); my seat is nudged
    64 px left, clear of the hand; the narrator speaks at the left, beside the table; docked trays sit at the
    bottom right (8 px), below the HUD column. — cost if wrong: a small table on landscape phones until Plan 11.
- Fit left for Plan 11 (all measured, none while paying): on landscape phones the bottom edge of my area dips
  a few px under the resting hand; at 568×320 the 2-player far seat touches the top; the discard, answer and
  counter trays can cover my seat and area at 1280×720, 1366×768 and 360×740 (none of those states picks
  table cards).

### Task 3: motion CSS on the switch — `refactor: key motion styles on the in-game switch, not the OS`
- `motion-css.test.ts` rewritten (6 tests); the plan's script (kept in the scratchpad) rewrote motion.css,
  tabletop.css and avatars.css (scene.css had no block left after Task 2). No `prefers-reduced-motion` remains.
- Reviewed the cascade: prefixed rules gained specificity (0,2,0+); each still only adds motion under `on` or
  cancels it under `off`, and the plain 100–200 ms micro-transitions keep their `off` → `none` pairs.
- Ruling: deleted the converted `.dapple` drift rule and its keyframes: they styled the light Task 2 removed.
  — dead CSS — cost if wrong: none.

### Task 4: the Animations switch — `feat: switch animations on or off from the HUD`
- `motion-control.test.tsx` (2); web 421. e2e +2 in `motion.spec.ts` ("cards fly even when the OS asks for less
  motion…", "every page carries the motion setting from the first paint"); 17 passed.
- Checked that the first-paint test fails with `applyMotion();` commented out (`Received: ""`), then restored.
- Ruling: with a fourth HUD control the phone HUD wrapped to two rows at 360 px and the narrator slid under it
  (`mobile.spec` caught it). Below 480 px the HUD's gaps and button padding are tighter (313 px wide at 360).
  — cost if wrong: none; 320 px phones still wrap (below the supported sizes).
- Ruling: on short landscape screens the HUD is a 2-column grid (the two toggles side by side, Game log and
  Leave game below), so it stays clear of the docked counter tray; the discard tray alone sits above the
  hand's right end (discarding picks hand cards). — cost if wrong: layout only.

### Task 5: the drag — `fix: keep a dragged card where it was grabbed, with no lag`
- `drag.test.tsx` +3 (grab point, 0×0 box, instant return with animations off); web 424.
- e2e +2 in `motion.spec.ts`: the plan's "a dragged card stays under the pointer where it was grabbed" (±2 px),
  and "a card dropped while it leans flies from where it leans, with no snap upright" (added, see ruling). 19 passed.
- Checked both can fail: with `drag.tsx` and `motion.css` stashed, "grabbed" is off by 93 px; with the plan's
  `data-rot={0}`, "leans" finds the flight 6° away from the leaning card.
- Ruling: the ghost's anchor is `data-rot="parent"`, not the plan's `data-rot={0}`. The lean turns the ghost
  up to 8°, and a dropped card's flight starts from the ghost's measured pose: with 0 the flight began
  upright, a visible 6° snap. `poseOf` already reads a parent's real turn for `"parent"`. — cost if wrong:
  none; the e2e test pins it.
- The grab fractions come from the card's bounding box at press time (a hovered hand card is lifted and
  slightly turned, so the box is the rotated one; within the ±2 px the e2e test allows).

### Task 6: visual and animation review (Step 1–2; no user wait, per the handoff)
Method: scratch scripts only (nothing committed): the test server with seed 18 for the home page, lobby and
table at 1440×900, 1280×720, 768×1024, 375×812 and 812×375; the scratch harness (real app routes fed with
engine-built transitions) for every peak, recorded with a CDP screencast into frame strips and audited frame by
frame (each clone's path, the hand-off to the real card, which CSS animations run).

What I saw, per viewport (fallback fonts where Google Fonts did not load):
- 1440×900 and 1280×720: navy ground, felt table with a thin rim at 22°; seats at the rim, HUD top right, hand
  and End turn clear. Cards are still the old small size (Plan 11). The home page is the paper card on navy.
- 768×1024: fits; the hover preview in the shot is the mouse left over the hand by the script, not a bug.
- 375×812: HUD one row (Sound, Animations, Game log, Leave game); table and seats fit; hand clear of End turn.
- 812×375: the short-landscape layout (column/grid HUD, left narrator, docked trays) fits; small table.
- No horizontal scroll anywhere; `data-motion="on"` on every page from the first paint.
- Lobby: at 22° the table grew taller and clipped the far chairs (Bob's avatar cut at the top). Fixed:
  `fix: fit the lobby table at the 22° tilt` (height `plane × 1.05 + 110px`, shift `−17% − 8px`), with an e2e
  check in `table.spec` that every chair shows in full (fails at −128 px without the fix). — Ruling (fit value).

Animations (motion on), 1440×900, 375×812 and 812×375: bank, set complete, Sly Deal, Forced Deal, Deal
Breaker, Just Say No, big rent, pay, win (confetti + banner), my turn (draw + "Your turn"), a player leaving.
- Every flight lands on its card (≤ 1 px on the real server's seeded game, desktop and phone); no clone left
  behind, no card left hidden, controls back after each batch (my own batches unlock in 0.35–0.9 s).
- CSS peaks run: shine and stamp (set complete, Deal Breaker), wheel and stamp (big rent), banner drop and
  confetti (win), ghost (leave), "Your turn" pulse, breathe on targets.
- Motion off: no clone and no peak anywhere; only the 150 ms card fade.
- Found and fixed (beyond the plan's tasks; the user asked for complete, bug-free animations — each test-first):
  - `fix: shudder an action a Just Say No cancels, from its first frame`: the known candidate from the phone
    audit. The cancelled action left the stage in the answer's own render, so the shudder never showed; and an
    effect due at 0 ms started on a timer, which let the stage blink out for ~3 frames. The stage now keeps the
    last action while the Just Say No effect runs, and effects due at once start with the batch (before paint),
    like flights leaving at 0 ms. — Ruling: behaviour change in `stage.ts` for every effect at 0 ms (the "Your
    turn" pulse, a set's shine…): they now start in the same frame as the batch. — cost if wrong: none seen;
    all stage and peak tests pass.
  - `fix: land a missed drag's card exactly on its place in the hand`: a card dropped off every zone flew home
    to the pose measured at release (the hand card still lifted by its hover, since the pointer was captured),
    then vanished 18–36 px off and up to 6° wrong, and the dimmed card faded back in (a flash). Now the return
    aims at the card's live pose on every frame and turns to its fan angle; the card waits hidden (not dimmed)
    under the ghost and they swap unseen. e2e: "a card dropped nowhere flies home and lands exactly on its place
    in the hand" (fails at 9 px on the Task 5 code). — Ruling: the lean spring is `stiffness 700, damping 50`
    (settles in ~150 ms, spec §6.2), not the plan's `300/30` (~270 ms). — cost if wrong: taste only.
- Checked and not bugs: the win scene's clones land exactly on the banner cards (a first probe matched them by
  name to the table cards underneath); two drawn cards with the same name were a probe artifact too.

Gates after the review fixes: web 427 (engine 108, protocol 6, server 59); typecheck, lint, build clean;
e2e 20 passed.

### Task 6: spec sync, gates, final review
- `docs: record Plan 10 as built in the layout spec` (§4 and §6.2 "As built (Plan 10)"), refreshed after the review.
- Final review (one fresh reviewer, most capable model): no Critical; 4 Important, all fixed test-first:
  1. The win confetti still followed the OS (`disableForReducedMotion`). `fix: throw win confetti whatever the OS asks about motion` (`confetti.test.ts`).
  2. The grab point drifted ~10 px on turned fan cards (fractions taken from the turned bounding box; the e2e
     test mirrored the code). Grab now in the card's own frame; the e2e test grabs the outermost card near a
     corner and computes its expectation independently (fails at 7.6 px on the old code).
     `fix: hold a turned fan card at the exact point it was grabbed`.
  3. The "dropped nowhere" e2e test was flaky (2.4 px in 1 of 4 runs): the return set poses from its own rAF,
     Motion painted a frame later. Reproduced at 4× CPU throttling (up to 3.8 px); the return now steps in
     Motion's frame loop and gives way one frame after its last pose: 0.46 px in 8/8 throttled runs.
     `fix: paint a returning card's every step in Motion's own frame`.
  4. With animations off a missed drop still held the ghost 260 ms (card hidden, next drag blocked).
     `fix: drop a missed drag at once and keep it upright with animations off` (also Minor 5: no lean when off).
  - Minor 6 fixed: the Just Say No stage read its action from a ref filled during render, which could be stale
    if two payloads arrived before one render. The `justSayNo` effect now carries the action it answers.
    `fix: carry the action a Just Say No answers in its effect`.
  - Minor 7 fixed: `fix: follow the Animations switch across open tabs` (the `reloadMotion` doc promised it),
    `docs: refresh the motion and table comments after Plan 10`.
- Ruling: the plan's unit test "sends a missed card home at once when animations are off" now asserts that the
  ghost is gone and the card shown at once (it pinned only the ghost's position). — Review Focus 5 — cost: none.
- Ruling: the return unit test runs on real time (Motion's frame loop ignores fake timers). — cost: ~0.4 s.
- Deferred minors:
  - a return started just before the tab hides finishes when the tab is visible again (rAF pauses);
  - if a hand card left the DOM in the very frame of its return, the ghost could flash for a frame toward
    (0,0) before the version change clears it;
  - "every page carries the motion setting from the first paint" checks the attribute, not literally frame 0
    (`applyMotion()` runs before `createRoot`, so it holds);
  - the fit gaps listed under Task 2 (Plan 11).
- Final re-check after the fixes (as the user asked: open, look, redo if wrong): every peak clean at 1440×900,
  375×812 and 812×375; CSS peaks run with animations on, only fades with them off; the Just Say No stage shakes
  from the first frame; a missed drop lands on its card within 1 px and 0°; the real server's seeded opening
  lands every flight within 1 px on desktop and phone; screenshots at all five viewports fit.
- Final gates: engine 108, protocol 6, server 59, web 432; typecheck, lint, build clean; e2e 20 passed
  (Chromium via the scratch config; `apps/e2e/playwright.config.ts` unchanged).

- PR: https://github.com/oplosy/monopoly/pull/11 (base `main`; not merged). PR #10 and `for_table/` untouched.
